<%= form_with(model: uniform, local: true) do |form| %>
  <% if uniform.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(uniform.errors.count, "error") %> prohibited this uniform from being saved:</h2>

      <ul>
      <% uniform.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name, id: :uniform_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
